// O .map serve para criar um novo array com base em outro array, retorna o msm //

const frutas = ['Goiaba', 'Maça', 'Banana']

const secondArray = frutas.map(frutas => {
  console.log('ablubleh');
})